import { Link } from "@tanstack/react-router";
import { useState,type ReactNode } from "react";
import { Menu, X, ShieldCheck, PhoneCall } from "lucide-react";
import { Button } from "@/components/ui/button";
import { LeadCapture } from "@/components/bookmyscan/LeadCapture";
const nav=[['Scan Tests','/scan-tests'],['Scan Centres','/scan-centres'],['Advanced Cancer Imaging','/advanced-cancer-imaging'],['Lu‑177 PSMA Therapy','/lu-177-psma-therapy'],['Scan Cost Guide','/scan-cost']] as const;
export function SiteShell({children}:{children:ReactNode}){const [open,setOpen]=useState(false);return <div className="min-h-screen bg-background text-foreground"><div className="bg-primary px-4 py-2 text-center text-xs text-primary-foreground">Compare prices only after confirming the prescribed protocol. Always consult your treating doctor.</div><header className="sticky top-0 z-50 border-b border-border bg-background/95 backdrop-blur"><div className="page-wrap flex h-18 items-center justify-between gap-5"><Link to="/" className="brand-mark" aria-label="BookMyScan.in home"><span>BookMy</span>Scan.in</Link><nav className="hidden items-center gap-5 xl:flex">{nav.map(([label,to])=><Link key={to} to={to} className="nav-link" activeProps={{className:"nav-link-active"}}>{label}</Link>)}</nav><div className="hidden lg:block"><Button asChild size="lg"><Link to="/book-a-scan"><PhoneCall/>Check Scan Price</Link></Button></div><Button variant="ghost" size="icon" className="xl:hidden" aria-label="Open menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</Button></div>{open&&<nav className="page-wrap grid gap-1 border-t border-border py-3 xl:hidden">{nav.map(([label,to])=><Link key={to} to={to} onClick={()=>setOpen(false)} className="rounded-md px-3 py-3 text-sm font-medium hover:bg-muted">{label}</Link>)}<Button asChild className="mt-2"><Link to="/book-a-scan" onClick={()=>setOpen(false)}>Check Scan Price</Link></Button></nav>}</header><main>{children}</main><footer className="bg-primary text-primary-foreground"><div className="page-wrap grid gap-10 py-14 md:grid-cols-2 lg:grid-cols-4"><div><div className="brand-mark brand-mark-inverse"><span>BookMy</span>Scan.in</div><p className="mt-4 text-sm leading-6 text-primary-foreground/75">Compare scan centres, review preferential rates and request appointment support across Delhi NCR.</p></div><FooterLinks title="Explore" links={[["Scan Tests","/scan-tests"],["Scan Centres","/scan-centres"],["Preparation Guides","/preparation-guides"],["Compare Scans","/compare-scans"]]}/><FooterLinks title="Delhi NCR" links={[["Delhi","/scan-centres/delhi"],["Gurgaon","/scan-centres/gurgaon"],["Noida","/scan-centres/noida"],["Faridabad","/scan-centres/faridabad"]]}/><div><h3 className="footer-title">Patient safety</h3><p className="text-sm leading-6 text-primary-foreground/75">Not for emergencies, diagnosis or treatment recommendations. In an emergency, call 112 or visit the nearest hospital.</p><div className="mt-4 flex items-center gap-2 text-xs"><ShieldCheck className="size-4 text-accent"/>Privacy-conscious assistance</div></div></div><div className="border-t border-primary-foreground/15"><div className="page-wrap flex flex-col gap-3 py-5 text-xs text-primary-foreground/65 md:flex-row md:justify-between"><p>© 2026 BookMyScan.in. Scan comparison and appointment-assistance platform.</p><p>Final prices and clinical decisions remain with centres and qualified doctors.</p></div></div></footer><LeadCapture/><div className="mobile-price-cta"><Button asChild size="lg"><Link to="/book-a-scan"><PhoneCall/>Check Scan Price</Link></Button></div></div>}
function FooterLinks({title,links}:{title:string;links:readonly (readonly [string,string])[]}){return <div><h3 className="footer-title">{title}</h3><div className="grid gap-2">{links.map(([label,to])=><Link key={to} to={to} className="text-sm text-primary-foreground/75 hover:text-primary-foreground">{label}</Link>)}</div></div>}
