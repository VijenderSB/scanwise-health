import {Link} from '@tanstack/react-router';import {ArrowRight} from 'lucide-react';import {PageIntro,Disclaimer} from './Blocks';
export function ContentHub({eyebrow,title,copy,items}:{eyebrow:string,title:string,copy:string,items:{title:string,slug:string,summary:string}[]}){return <><PageIntro eyebrow={eyebrow} title={title} copy={copy}/><section className="section page-wrap"><Disclaimer/><div className="card-grid mt-8">{items.map(i=><Link key={i.slug} to="/guides/$slug" params={{slug:i.slug}} className="category-card"><h2 className="text-xl">{i.title}</h2><p>{i.summary}</p><strong>Read guide <ArrowRight/></strong></Link>)}</div></section></>}
