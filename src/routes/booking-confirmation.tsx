import {createFileRoute,Link} from '@tanstack/react-router';import {z} from 'zod';import {zodValidator,fallback} from '@tanstack/zod-adapter';import {CheckCircle2,MessageCircle} from 'lucide-react';import {Button} from '@/components/ui/button';const schema=z.object({reference:fallback(z.string(),'').default('')});export const Route=createFileRoute('/booking-confirmation')({validateSearch:zodValidator(schema),head:()=>({meta:[{title:'Enquiry Received | BookMyScan.in'},{name:'description',content:'Your scan appointment-assistance request has been received.'},{property:'og:title',content:'Enquiry Received'},{property:'og:description',content:'BookMyScan.in appointment assistance.'},{name:'robots',content:'noindex'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Page});function Page(){const {reference}=Route.useSearch();return <section className="section page-wrap"><div className="mx-auto max-w-xl text-center"><CheckCircle2 className="mx-auto size-16 text-accent-foreground"/><p className="eyebrow mt-6">Request received</p><h1 className="text-4xl">We’ll help coordinate the next step.</h1><p className="lead mx-auto">Your reference is <strong>{reference||'being generated'}</strong>. A coordinator can use the details you shared to identify suitable centre options.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><Button asChild><Link to="/scan-tests">Continue exploring</Link></Button><Button variant="outline" disabled title="Business WhatsApp number pending verification"><MessageCircle/>WhatsApp pending setup</Button></div><p className="mt-6 text-xs text-muted-foreground">This is not medical advice or an appointment confirmation. Always consult your treating doctor.</p></div></section>}
