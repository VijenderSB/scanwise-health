import {createFileRoute} from '@tanstack/react-router';import {z} from 'zod';import {zodValidator,fallback} from '@tanstack/zod-adapter';import {PageIntro,Disclaimer} from '@/components/bookmyscan/Blocks';import {BookingForm} from '@/components/bookmyscan/BookingForm';const searchSchema=z.object({scan:fallback(z.string(),'').default(''),protocol:fallback(z.string(),'').default(''),centre:fallback(z.string(),'').default(''),offer:fallback(z.string(),'').default('')});export const Route=createFileRoute('/book-a-scan')({validateSearch:zodValidator(searchSchema),head:()=>({meta:[{title:'Compare & Book a Radiology Scan | SavoScan.com'},{name:'description',content:'Share your prescription, compare confirmed scan totals and request radiology appointment support across Delhi NCR.'},{property:'og:title',content:'Check Scan Price & Availability'},{property:'og:description',content:'Securely share your scan details and preferred location.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}],links:[{rel:'canonical',href:'/book-a-scan'}]}),component:Page});function Page(){const {scan,protocol,centre,offer}=Route.useSearch();return <><PageIntro eyebrow="Price and appointment support" title="Find your scan offer." copy="Tell us the exact scan or upload your prescription. We’ll confirm an equivalent protocol, total payable amount and suitable centre before you book."/><section className="section page-wrap"><div className="mx-auto max-w-4xl"><Disclaimer/><BookingForm defaultScan={scan} defaultProtocol={protocol} defaultCentre={centre} defaultOffer={offer}/></div></section></>}
