import {createFileRoute,redirect} from '@tanstack/react-router';import {centres} from '@/lib/catalog';export const Route=createFileRoute('/scan-centres/$slug')({beforeLoad:({params})=>{const c=centres.find(x=>x.slug===params.slug);if(c)throw redirect({to:'/scan-centres/$city/$slug',params:{city:c.city.toLowerCase().replace(' ','-'),slug:c.slug}});throw redirect({to:'/scan-centres'})}});
